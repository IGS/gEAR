'use strict';

window.onload=function() {
    // Set the page title
    document.getElementById('page-header-label').textContent = 'Hair Cell Paper Landing page';
};

const handlePageSpecificLoginUIUpdates = async (event) => {
    
}