'use strict';

window.onload=function() {
    // Set the page title
    document.getElementById('page-header-label').textContent = 'Neocortex Devo Landing page';
};

const handlePageSpecificLoginUIUpdates = async (event) => {
    
}